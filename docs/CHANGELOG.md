# CHANGELOG — ცვლილებების ჟურნალი

**ეს ფაილი ერთადერთი ადგილია, სადაც ბოლო ცვლილებები იწერება.** ახალი ჩანაწერი ყოველთვის **თავში**
ემატება. `CLAUDE.md` მხოლოდ მიმდინარე მდგომარეობას აღწერს — ისტორია აქ ცხოვრობს.

**ჩანაწერის წესი, რომელიც აზრს ინარჩუნებს:**
* დაწერე **ძირეული მიზეზი**, არა მხოლოდ სიმპტომი — რატომ მოხდა, არა მხოლოდ რა შეიცვალა
* ცალკე მონიშნე, **რა არის დამტკიცებული და რა არა.** გადაუმოწმებელი ფიქსი ცხადად უნდა ეწეროს
  „UNVERIFIED"-ად, თორემ შემდეგი სესია მას დადასტურებულად ჩათვლის
* ჩაწერე გეითის რეალური ციფრი (`pytest`, `evals`), არა „ტესტები გადის"
* ჩაწერე ისიც, რაც **არ არის ბაგი** — თორემ ვიღაც მას „გაასწორებს"

---

## 2026-07-31 (მოგვიანებით) — arc-ის კოდი ცოცხალი არ იყო · ლოგირება გამორთული აღმოჩნდა · deploy `08bf5e6d`

**გეითი:** `pytest tests/ -q` = **1 failed / 5555 passed / 28 skipped** (633წმ). ერთადერთი ჩავარდნა
იგივე წინასწარ არსებული `test_approved_copy_service_2026_07_11::test_parent_flow_start_book_intent_uses_fast_track_and_advances_state`-ია.
**რეგრესია: 0.** ცვლილება მხოლოდ `app/main.py` (+44 ხაზი, წაშლის გარეშე).

### ⭐ ძირეული მიზეზი — ლოგირება არასოდეს ყოფილა კონფიგურირებული

აპლიკაციაში `logging.basicConfig` / `setLevel` **არსად არ გამოიძახებოდა**. Python-ი `logging.lastResort`-ს
იყენებდა — **WARNING-only stderr** handler-ს. ანუ ამ კოდბეისში დაწერილი **ყველა** `logger.info`
დიაგნოსტიკა პროდაქშენში ჩუმად იკარგებოდა:

* `[slot_check] requested_datetime=… / inside_business_hours=… / available=False reason=…`
* `[book_consultation] datetime_parsed=… / slot_available=…`
* `[messenger_debug] turn … in=%r out=%r` (`webhook.py:508`) — ყოველი ბრუნის შესატანი და პასუხი

გადარჩა მხოლოდ `print()` — ამიტომ ჩანდა `[SENT][messenger]` (`messenger_service.py:217`).
**ამიტომ ცოცხალი ჯავშნის ჩავარდნა Railway-ის ლოგებიდან საერთოდ ვერ იკვლეოდა.** ფიქსი ახალ ლოგს **არ**
ამატებს — მხოლოდ არსებულებს უხსნის გამოსასვლელს. `basicConfig` no-op-ია, თუ handler უკვე დგას
(pytest-ის caplog), ამიტომ ტესტების ჩაჭერა ხელუხლებელია. third-party logger-ები (`httpx`, `google`,
`openai`, `anthropic`, …) ცხადად WARNING-ზეა — მათი INFO request-URL-ს ბეჭდავს („არასოდეს დაალოგო ტოკენი").

### deploy `d3eef9db` გაჭედილი იყო — arc-ის ორი მთავარი ფიქსი ცოცხალი არ ყოფილა

`d3eef9db` **65+ წუთი** `INITIALIZING`-ში იდგა, `snapshotId=null`, არც ერთი build/deploy ლოგი.
ტრაფიკს ისევ `2f1055f1` (PR #17, `7a44f6b4`) ემსახურებოდა. git ancestry: `7ce29df` და `1e20a4d`
**იყო** მასში, `cabfa84` და `3f3adee` — **არა**.

> **⚠️ შესწორება წინა ჩანაწერზე:** „შემდეგი სესია აქედან იწყებს" პუნქტი 1 არასწორ მარკერს იყენებდა.
> `⚙️ USE_CONSULTATION_PROGRAM_NAME=True` **უკვე ეწერა PR #17-ის boot-ლოგში** (09:47:19), ამიტომ
> arc-ის კოდს **ვერ ამტკიცებს**. ახალი სანდო მარკერია **`⚙️ LOG_LEVEL=…`** — ის ამ commit-ამდე
> არ არსებობდა.

> **⚠️ Railway-ის `redeploy` API-ს ხაფანგი:** გამოძახებამ დააბრუნა შეცდომა
> („Cannot redeploy without a snapshot"), **მაგრამ მაინც გაუშვა** ძველი snapshot-ის deploy
> (`bc5d3dec`, PR #17) — production ერთი წუთით ძველ კოდზე დაბრუნდა. გამოსავალი იყო commit-ის
> ცხადად მითითებით ახალი deploy: **`08bf5e6d` = SUCCESS, commit `6c9c8bd9`**.

### ცოცხალი ტესტი — რა დამტკიცდა და რა არა

**✅ `cabfa84` მუშაობს.** 12:32:42 — „ნიკოლოზ გობეჯიშვილი 595999733 14 წელი" ერთ მესიჯად, სწორად
წაკითხული, ხელახლა არ უკითხავს. ძველ ბილდზე იგივე სცენარი 09:52:21 და 09:53:57-ზე „კიკი"-ს იძლეოდა.

**❌ markdown-ის ფიქსი (`3f3adee`) — „⛔ UNVERIFIED" ახლა უარყოფილია.** ახალ ბილდზე 12:33:10 და
12:33:32 პასუხები კვლავ `*`-ს ატარებს (`- *სახელი:*`, `*31 ივლისი — 19:00*`). მიზეზი: `reply_rendering=plain_text`
მხოლოდ **მინიშნებაა**, თვითონ პრომპტები კი `*`-ით არის გაჯერებული — **398 შემთხვევა 12 ფაილში,
მარტო `system_parent_v2.md`-ში 198 ხაზი**. წესის შესაბამისი ფიქსი = **პრომპტებიდან წაშლა**, არა აკრძალვა.

**❌ ჯავშანი დადასტურებაზე ვარდება — ძირეული მიზეზი ჯერ დაუდგენელია.** 12:33:10 „ხვალ 12:00
თავისუფალია!" → „კი" → 12:33:32 „ეს თარიღი ვერ ვიპოვე სისტემაში" + 3 ალტერნატივა. ეს ფრაზა
**რეპოში არ არსებობს** — მოდელმა გამოიგონა. `book_consultation`-ის `slot_unavailable` WARNING-ად
იწერება და ეს ხაზი მთელ დღეში არსად არის, ამიტომ უარი სავარაუდოდ `check_consultation_slot`-იდან
მოვიდა — რომლის უარის ხაზები INFO-შია, ანუ იმ დროს უხილავი. **ჰიპოთეზა, დაუმტკიცებელი.**

**❌ ცარიელი პასუხი.** 12:34:52 — `[parent_llm_engine] empty final content with no tool calls` →
`webhook.py:515` `if not response: return`. **fallback არ არსებობს** — მომხმარებელი დუმილს იღებს.

**❌ მენეჯერის ნომერზე უარი — 4-ჯერ** (10:03:07, 10:03:32, 10:04:16, 11:19:51), **ყველა ძველ ბილდზე**.
ორი დამოუკიდებელი ხვრელი:
1. `_is_explicit_manager_number_request` მოითხოვს მენეჯერის სიტყვას **და** `ნომერ`/`ტელეფონ`/`კონტაქტ`-ს
   **და** მესიჯში ტელეფონის არარსებობას. სხვა ფორმულირება LLM-თან მიდის და ის უარს იგონებს —
   `_render_manager_number_answer` თვითონ **არასოდეს** ამბობს უარს.
2. `get_manager_phone()` **მხოლოდ** Admin Config-ს კითხულობს (ვოლუმის `sections.yaml` →
   `manager_contacts` mirror) და `MANAGER_PHONE_NUMBER`-ს **განზრახ უგულებელყოფს** — ცარიელს აბრუნებს.
   ლოკალურად `set`-ია; **ვოლუმის მნიშვნელობა გადამოწმებული არ არის.** ამიტომ დაემატა boot-ხაზი
   `⚙️ manager_phone=set|NOT set` (მნიშვნელობის გარეშე).

### ⚠️ რაც **არ არის** დამტკიცებული

* დისნეილენდის პასუხს (`*4 000 ₾*`) ლოგებში `[SENT]` **არ შეესაბამება** — ბილდზე ვერ მიეკუთვნება.
  markdown-ის დასკვნა ეყრდნობა **P008/P010-ს**, რომლებიც ზუსტად ემთხვევა 12:33:10-ს და 12:33:32-ს.
* `USE_PROGRAM_FOLLOWUP`-ის მნიშვნელობა ისევ უცნობია (boot-ში არ იბეჭდება; OAuth მალავს).

### შემდეგი სესია აქედან იწყებს

1. boot-ლოგში ნახე **`⚙️ LOG_LEVEL=INFO`** (ახალი კოდის მტკიცებულება) და **`⚙️ manager_phone=…`**
2. გაიმეორე ჯავშნის სცენარი და წაიკითხე უკვე **ხილული** `[slot_check]` / `[book_consultation]` ხაზები
   — რატომ დაბრუნდა `available=False` ხვალ 12:00-ზე
3. `[messenger_debug] turn … in=…` — ზუსტი ფრაზა, რომელზეც მენეჯერის დეტექტორმა არ იმუშავა
4. markdown: `*` პრომპტებიდან **წაშალე** (398 შემთხვევა), არა აკრძალვით
5. `webhook.py:515`-ის ცარიელი პასუხის fallback
6. `/data/admin_config/sections.yaml`-ის ჩამოტანა ვოლუმიდან (ისევ ღიაა)

---

## 2026-07-31 — live-test arc · commits `7ce29df` → `4580c81` · PR #18 · deploy `d3eef9db`

**გეითი (ყოველ commit-ზე):** `pytest tests/ -q` = **1 failed / 5555 passed / 28 skipped**.
ერთადერთი ჩავარდნა — `test_approved_copy_service_2026_07_11::test_parent_flow_start_book_intent_uses_fast_track_and_advances_state`
— **წინასწარ არსებულია და დაუკავშირებელი**. `python -m evals.run_evals` = **93/100** (offline, უცვლელი).
`--llm --judge` = **84/100** · 🇬🇪 125/132 — მაგრამ იხილე „რაც არ არის ბაგი" ქვემოთ.

**ოპერატორის მბოჭავი წესი, რომელსაც მთელი arc დაემორჩილა:** არასოდეს გაასწორო აკრძალვის დამატებით ან
ახალი დეტერმინისტული path-ით — სწორედ ამან აქცია ერთი ბაგი 2–10-ად. ყოველი ცვლილება არის **წაშლა**,
**სიმეტრიის აღდგენა**, ან **ფაქტის მიწოდება**, რომელიც მოდელს აკლდა. არცერთი banned-phrase არ დამატებულა.

### ⭐ `cabfa84` — Anthropic-ის თარგმნის ფენა მიმდევრობით ბრუნებს აწებებდა

`anthropic_service._normalize_turns` მიმდევრობით ერთნაირი როლის ბრუნებს აერთიანებს (Anthropic
მოითხოვს როლების მონაცვლეობას; OpenAI არა). გაერთიანება ხდებოდა `content.extend(...)`-ით, ხოლო ორ
მიმდევრობით **ტექსტურ** ბლოკს მოდელი განუყოფლად კითხულობს — ანუ ბრუნის საზღვარი ჩუმად იშლებოდა:

```
„კი" + „კი"                                     ->  „კიკი"
„ნიკოლოზ გობეჯიშვილი" + „595999733" + „14 წელი" ->  „ნიკოლოზ გობეჯიშვილი59599973314 წელი"
```

**ერთმა ამ დეფექტმა სამივე ცოცხალი სიმპტომი გამოიწვია.** `.docx`-ის run-ების ანალიზმა დაამტკიცა, რომ
მომხმარებელმა გააგზავნა ზუსტად `კი` (ერთი run), აგენტმა კი დააბრუნა `კიკი` (ერთი run) — გაორმაგება
ჩვენს კოდშია:
* დადასტურება სახელის შეცვლად წაიკითხა → **ჯავშანი არასოდეს დასრულდა**
* სახელი, ნომერი და ასაკი ერთ გაუშიფრავ სტრიქონად მივიდა → **აგენტი თავიდან ითხოვდა უკვე მოცემულს**

ფიქსი: მიმდევრობითი **ტექსტური** ბლოკები `\n`-ით იყოფა; არა-ტექსტური ბლოკები (`tool_result`
multi-tool რაუნდის შემდეგ) ხელუხლებელია.

> **დასამახსოვრებელი:** ეს provider-switch-ის რეგრესიაა — gpt-4.1-mini-ზე არ არსებობდა. როცა სიმპტომი
> „ახალია `LLM_PROVIDER=anthropic`-ის შემდეგ", **ჯერ ეს ფენა ეჭვმიტანე, მერე პრომპტი.**

### `3f3adee` — per-program follow-up მიუწვდომელი იყო + პასუხის მედია არასოდეს იყო ნათქვამი

**Follow-up მკვდარი იყო ადმინ-პანელის ყველა პროგრამაზე.** `followup_service._followup_program_eligibility`
ადრევე ბრუნდებოდა `segment == "PARENT"`-ზე (`is_dynamic=False`) და ჩრდილავდა ქვემოთ მდებარე
`program_id`-ის შემოწმებას. დისნეილენდი და საკვირაო სკოლა PARENT ფლოუშია, ამიტომ ისინი „ბანაკის ლიდად"
კლასიფიცირდებოდნენ და ბანაკის რეგისტრაციის კარიბჭე ჩუმდებოდა მათ, როგორც კი ოპერატორმა ბანაკი დახურა.

გაზომილი, ფიქსამდე: დისნეილენდის ლიდი, 25სთ უმოქმედო, `USE_PROGRAM_FOLLOWUP=true` →
`[followup] skipped reason=camp_registration_closed`. ფიქსის შემდეგ: **`sent`, `stage → first_24h`**.
ბანაკის გზა ხელუხლებელია; flag-OFF byte-identical.

> ⚠️ `USE_PROGRAM_FOLLOWUP` **boot-ლოგში არ იბეჭდება.** დაადასტურე, რომ Railway-ზე `true`-ა, თორემ
> ფიქსი უმოქმედოა.

**აგენტი Markdown-ს აგზავნიდა Meta-ზე.** ცოცხალი: `**კონსულტაცია წარმატებით დაიჯავშნა!**` და
`| … |---|` ცხრილი — Messenger სიმბოლოებს ლიტერალურად აჩვენებს. არცერთი ცოცხალი პრომპტი არ ამბობდა,
რა მედიაზე მიდის პასუხი, თვითონ პრომპტები კი **537 `*`-ს და 13 `**`-წყვილს** ატარებენ, რომელსაც
მოდელი იმეორებს. per-turn კონტექსტს დაემატა `reply_channel` + `reply_rendering=plain_text`.
**⛔ UNVERIFIED — ცოცხალ მოდელზე არ შემოწმებულა.**

### `7ce29df` — ბუქინგის ოთხი დეფექტი, დამტკიცებული 2026-07-29/30-ის Railway-ლოგებით

1. **ლიტერალური მაგალით-თარიღები მოიხსნა** (15 ხაზი: 14 `system_parent_v2.md`, 1 `parent_lean.md`).
   „27 მაისს" 9-ჯერ ჩნდებოდა და პრომპტის ყველაზე გამეორებადი სტრიქონი იყო; მოდელი მას პირდაპირ
   გადასცემდა ხელსაწყოს → `[book_consultation] BLOCKED reason=datetime_in_past slot=2026-05-27T10:00`.
   მაგალითები ახლა ფარდობითია („ხვალ"), რომელსაც backend ხსნის — ანუ კოპირებაც კი ვალიდურ მომავალ
   თარიღს იძლევა. ტესტი ჩავარდება, თუ ლიტერალური ქართული თვე+რიცხვი დაბრუნდება ცოცხალ პრომპტში.
2. **წარსული დღე სრულ სლოტებს აბრუნებდა.** `calendar_service._get_free_slots_for_day` `SLOT_BUFFER`-ს
   მხოლოდ მაშინ იყენებს, როცა `target_date == today`, ამიტომ ადრინდელი თარიღი მომავალივით სკანირდებოდა.
   ცოცხალი: 29 ივლისს შესთავაზა „30 ივნისს" სლოტები, მერე კი *იმავე სიიდან* არჩეული უარყო როგორც
   წარსული. **RED-შემოწმებული:** guard-ის გარეშე `2026-06-30` აბრუნებს **11** სლოტს; guard-ით — **0**.
3. **სამუშაო საათები მხოლოდ პრომპტში იყო.** პრომპტი ორჯერ წერს 10:00–21:00-ს, მაგრამ ორივე ჩანაწერი
   52k-სიმბოლოიან ბლობში ~15%-ზეა; ცოცხლად მოდელმა გამოაცხადა „10:00 – 17:00" (ციფრი, რომელიც **რეპოში
   არსად არსებობს**) და უარყო 17:00, რომელსაც executor მიიღებდა. ახლა `booking_hours_tbilisi` +
   `booking_slot_minutes` per-turn კონტექსტში მოგზაურობს თარიღების გვერდით, `calendar_service`-იდან —
   ანუ ვერასოდეს დაშორდება იმას, რასაც executor აღასრულებს.
4. **მომხმარებლის საკუთარი ჯავშანი კონფლიქტად ითვლებოდა.** 10:00-ზე ჩაწერის შემდეგ მშობელმა სთხოვა
   საკვირაო სკოლაც იმავე შეხვედრაზე; ხელახალი შემოწმება მის *საკუთარ* კონსულტაციას დაეჯახა, executor-მა
   დააბრუნა უბრალო `calendar_busy` → აგენტმა უთხრა „ვეღარ ხელმისაწვდომია" მისსავე შეხვედრაზე და ორჯერ
   მოიხადა ბოდიში. ახალი `_is_own_existing_booking` (`_is_reschedule_scenario`-ს სარკე) აბრუნებს
   `already_booked_by_this_lead`-ს, ალტერნატივების გარეშე. ერთი კონსულტაცია რამდენიმე პროგრამას ფარავს.

ახალი ფაილი `tests/test_live_booking_fixes_2026_07_30.py` (10 ტესტი) აფიქსირებს **ფაქტს**, რომელშიც
თითოეული დეფექტი ცდებოდა, არა ფორმულირებას. ორივე ახალი guard **RED-შემოწმებულია** — გამორთვისას
ძველი ქცევა ბრუნდება.

### `1e20a4d` — სუიტა ახლა **კოდს** აღწერს, არა ოპერატორის `.env`-ს

`.env` სერვერის boot-ლოგზე გასწორდა და **13 ტესტი გაწითლდა** — არცერთი არ ამოწმებდა იმას, რასაც
აცხადებდა. `conftest` `USE_PER_PRODUCT_BOOKING=False`-ს აჭერდა config/parent_flow/conversation_service/
ორივე engine-ზე, მაგრამ **არა `sheets_service`-ზე**, რომელსაც საკუთარი `settings` აქვს და ამ flag-ით
CRM-ის რიგის სიგანეს ირჩევს (A1:Q1 vs A1:R1). ოთხ smart-agent flag-ს (`USE_DYNAMIC_PROGRAMS`,
`USE_SKILLS`, `USE_LEARNING`, `USE_LEAD_MEMORY`) საერთოდ არ ჰქონდა pin.
`test_flag_from_env_default_off` მხოლოდ პროცესის ცვლადს შლიდა — `_env` კი `.env`-ზე გადადის, ანუ ეს
იზოლაცია არასოდეს ყოფილა.

`test_agent.py` (დამოუკიდებელი, `pytest tests/`-ით არ იკრიბება): სლოტების ფიქსტურა `2026-05-15`-ზე იყო
ჩაწებებული (date-bomb) → ახლა საათზეა მიბმული; ყალბი `settings` ხელით დაწერილი `SimpleNamespace` იყო და
`AttributeError`-ს იძლეოდა ყოველ ახალ flag-ზე (`USE_SECTION_STATUS_GATE`), რაც ჩუმად აზრს უკარგავდა
ADULT-ის 6 შემოწმებას → ახლა რეალურ `Settings`-ის default-ებზე გადადის.

### `1075fbc` + `4580c81` — დოკუმენტაცია (და ერთი აღდგენილი შეცდომა)

`CLAUDE.md`-ს თავში დაემატა `CURRENT STATE — 2026-07-31`. `1075fbc`-მ HANDOFF.md **3259 ხაზიდან 187-მდე
მოჭრა** PowerShell-ის round-trip-ის გამო — ეს მონაცემის დაკარგვა იყო, არა განზრახული რედაქტირება.
`4580c81`-ით HANDOFF.md **ბაიტ-იდენტურად აღდგა** (git-თან შედარებით გადამოწმებული), arc-ის handoff კი
ცალკე ფაილში გადავიდა: `docs/HANDOFF_LIVE_TEST_ARC_2026_07_31.md`.

---

### ⚠️ რაც **არ არის ბაგი** — ნუ „გაასწორებ"

**ბანაკი ოპერატორის გადაწყვეტილებით არის დახურული.** ყველა ნაკადი წარსულშია და რეგისტრაცია დახურულია
**განზრახ**. შესაბამისად:
* ბანაკზე ნებისმიერი კითხვა სწორად პასუხობს „ბანაკის ბოლო ნაკადი უკვე დაიწყო და რეგისტრაცია დასრულებულია."
* `test_agent.py` ვარდება **31/68** ზუსტად ამ ერთი მიზეზით — მისი მოლოდინები დაწერილია მაშინ, როცა
  ბანაკი ცოცხალი იყო. **`CLAUDE.md`-ის ძველი ჩანაწერი „68 mocked end-to-end checks ✅" მოძველებულია.**
* `--llm --judge` აჩვენებს **84/100**-ს, მაგრამ მისი **23 ჩავარდნიდან 14 ეს ერთი ხაზია**
  (`[U7] actual: [0/3] sample: ბანაკის ბოლო ნაკადი…`). ანუ 84/100 განზრახ დახურულ ძაბრს ზომავს, არა
  აგენტის მსჯელობას. ციფრს ნდობა ჯერ ნაადრევია — eval-ის ქეისები ცოცხალ პროგრამებზე უნდა გადამისამართდეს.
* ცოცხალი პროგრამები ახლა მხოლოდ **დისნეილენდი** + **საკვირაო სკოლა**-ა (ზრდასრულთა ღონისძიებებიც გამორთულია).

### ⛔ რაც **დამტკიცებული არ არის** — ნუ ჩათვლი გასწორებულად

1. **Markdown-ის ფიქსი ცოცხალ მოდელზე არ შემოწმებულა.** ლოგიკა სწორია; შედეგი უცნობი.
2. **თარიღის დეფექტი შეიძლება დარჩენილი იყოს.** ცოცხალი docx: „ხვალ 12:00 თავისუფალია" → დადასტურებაზე
   → „სისტემა წარსულ თარიღად ხედავს". „ხვალ" მომავალია, ანუ პასუხი არასწორია. **ჰიპოთეზა:** „კიკი"-ს
   კორუფციამ მოდელს თარიღის ძაფი დააკარგვინა, ანუ `cabfa84` შეიძლება უკვე ფარავს — **დაუმტკიცებელი**.
3. **`summary.md` გადაწყობილია, მაგრამ CRM-ის „Conversation Summary"-ის გაბერვა არასოდეს გამეორებულა**
   (12 ცოცხალი გაშვება claude-sonnet-4-6-ზე, ძველი და ახალი პრომპტი — ყველა სუფთა). შენარჩუნებულია
   იმიტომ, რომ კონტრაქტი უკეთ არის აღწერილი, **არა იმიტომ, რომ დამტკიცდა**.

### გარემო

* `.env` ახლა Railway-ის boot-ლოგს ემთხვევა (14/14 flag). ხუთი სხვაობდა, ოთხი ლოკალურად **გამორთული**
  იყო — ანუ ყველა ადრინდელი ლოკალური ტესტი სხვა აგენტს ამოწმებდა.
* **`pytest` განზრახ ჰერმეტულია და ვერასოდეს გაჩვენებს, სად არის ცოცხალი აგენტი** — ეს სწორია და ასე
  უნდა დარჩეს. სერვერს ასახავს **eval-ის ჰარნესი** (ცოცხალი settings-იდან იწყებს `dataclasses.replace`-ით).
* Railway MCP დაკავშირებულია (user scope). პროექტი `brilliant-vision`
  `fae52cbf-2960-45fb-bfd6-2908aebc9740`, სერვისი `web` `38c9f623-8555-423d-a5f6-3f2fa420de30`,
  გარემო `production` `82c8602a-ed1f-4a43-bc5e-bd4e35c69a18`. **OAuth ⇒ `list-variables` მხოლოდ
  სახელებს აბრუნებს** (`valuesRedacted: true`) — flag-ების **მნიშვნელობები boot-ლოგიდან იკითხება**.
* `app/agent/skills/*.md` არის **მეოთხე ცოცხალი პრომპტის ფენა** (`USE_SKILLS=true`), რომელიც პრომპტების
  აუდიტს არასოდეს დაუფარავს. ასევე ცოცხალია და აქ დაუდოკუმენტირებელი: `USE_DYNAMIC_PROGRAMS`,
  `USE_PER_PRODUCT_BOOKING`, `USE_DYNAMIC_WELCOME`.

### მეთოდის შენიშვნები, რომლებმაც ამ arc-ში ანაზღაურა

* **ჯერ ლოგები ითხოვე.** Railway-ის ლოგებმა და ცოცხალმა `.docx` ტრანსკრიპტმა იპოვა ძირეული მიზეზები,
  რომლებიც სქრინებმა და მსჯელობამ ვერ იპოვა.
* **shell-ის pipe-ში გატარებული სიმბოლო-დონის ნებისმიერი მტკიცება დაუშვებელია** — ქართული cp1252-ად
  ფუჭდება. ფაილი Python-ით წაიკითხე, ცხადი UTF-8-ით.
* **RED-შემოწმება.** ტესტი, რომელიც ფიქსის გარეშეც მწვანეა, უსარგებლოა — გამორთე guard და დარწმუნდი,
  რომ ძველი ქცევა ბრუნდება.

### შემდეგი სესია აქედან იწყებს

1. `d3eef9db`-ის boot-ლოგი — არის თუ არა `⚙️ USE_CONSULTATION_PROGRAM_NAME=…` (ახალი ხაზი ⇒ ახალი კოდი
   ცოცხალია; არარსებობა ⇒ ძველი კონტეინერი) და ჩართულია თუ არა `USE_PROGRAM_FOLLOWUP`
2. markdown-ის შემოწმება ცოცხალ პასუხზე
3. თარიღის სცენარის ხელახალი გატარება
4. `/data/admin_config/sections.yaml`-ის ჩამოტანა Railway-ის ვოლუმიდან — მის გარეშე დისნეილენდი და
   საკვირაო სკოლა ლოკალურად არ არსებობს
